<!DOCTYPE html>
<html>
<head>
    <title>{{ title }}</title>
</head>
<body>
    <h1>Users List</h1>
    <ul>
        {% for user in users %}
            <li>
                <a href="/users/{{ user.id }}">{{ user.name }}</a> ({{ user.email }})
            </li>
        {% else %}
            <li>No users found.</li>
        {% endfor %}
    </ul>
</body>
</html>